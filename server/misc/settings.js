const tokenCookieName = "store1";
module.exports.tokenCookieName = tokenCookieName;

const cookieMaxAge = 1 * 24 * 60 * 60 * 1000 ;
module.exports.cookieMaxAge = cookieMaxAge;

const tokenPassword = "mySecretPassword";
module.exports.tokenPassword = tokenPassword;